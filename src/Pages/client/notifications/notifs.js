import ClientHeader from "../home/header";
import "./notifs.css";

export default function Notifications() {
  return (
    <section id="client-cont">
      <ClientHeader />
      <section id="client-notifs">notifications</section>
    </section>
  );
}
